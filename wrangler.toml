name = "telmo-partilhas"
pages_build_output_dir = "."
compatibility_date = "2026-09-11"

# D1 é ligado no deploy com:
# npx wrangler pages deploy . --d1 DB=80d0977f-5cd6-4efa-9e6e-e2656b6f9e59
#
# O binding R2 MEDIA deve ser ligado no Cloudflare Pages:
# Settings > Functions > Bindings > R2 bucket bindings
# O nome do bucket não foi fornecido, por isso não existe nenhum placeholder inválido aqui.
