name = "telmo-partilhas"
compatibility_date = "2026-09-11"
pages_build_output_dir = "."

# Cloudflare Pages + Pages Functions.
# Bindings obrigatórios no Cloudflare Dashboard:
# DB    -> D1 existente com ID 80d0977f-5cd6-4efa-9e6e-e2656b6f9e59
# MEDIA -> R2 bucket para imagens/vídeos
# Secret -> ADMIN_PASSWORD

# O database_name da D1 não foi adivinhado.
# A associação da base pelo Dashboard evita colocar um nome inventado no ficheiro.
