import{json,isAdmin}from'./_utils.js';
async function ensureTable(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS app_state (id INTEGER PRIMARY KEY CHECK (id = 1), data TEXT NOT NULL, updated_at TEXT NOT NULL)`).run()}
export async function onRequestGet({env}){if(!env.DB)return json({ok:false,error:'D1 não está ligado a este projeto.'},503);try{await ensureTable(env);const row=await env.DB.prepare('SELECT data,updated_at FROM app_state WHERE id=1').first();if(!row)return json({ok:true,data:null,updatedAt:null});return json({ok:true,data:JSON.parse(row.data),updatedAt:row.updated_at})}catch(e){return json({ok:false,error:String(e?.message||e)},500)}}
export async function onRequestPut({request,env}){if(!(await isAdmin(request,env)))return json({ok:false,error:'Não autorizado.'},401);if(!env.DB)return json({ok:false,error:'D1 não está ligado a este projeto.'},503);let data;try{data=await request.json()}catch{return json({ok:false,error:'JSON inválido.'},400)}const now=new Date().toISOString();await env.DB.prepare('INSERT INTO app_state(id,data,updated_at) VALUES(1,?,?) ON CONFLICT(id) DO UPDATE SET data=excluded.data,updated_at=excluded.updated_at').bind(JSON.stringify(data),now).run();return json({ok:true,updatedAt:now})}
