import{json,isAdmin}from'./_utils.js';
async function ensureTable(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS messages (id TEXT PRIMARY KEY,date TEXT NOT NULL,name TEXT NOT NULL,email TEXT NOT NULL,subject TEXT,message TEXT NOT NULL,read INTEGER NOT NULL DEFAULT 0)`).run()}
export async function onRequestPost({request,env}){if(!env.DB)return json({ok:false,error:'D1 não está ligado a este projeto.'},503);let body;try{body=await request.json()}catch{return json({ok:false,error:'JSON inválido.'},400)}if(!body.name||!body.email||!body.message)return json({ok:false,error:'Campos obrigatórios em falta.'},400);await ensureTable(env);const id=`${Date.now()}-${crypto.randomUUID()}`;await env.DB.prepare('INSERT INTO messages(id,date,name,email,subject,message,read) VALUES(?,?,?,?,?,?,0)').bind(id,body.date||new Date().toISOString(),String(body.name),String(body.email),String(body.subject||''),String(body.message)).run();return json({ok:true,id})}
export async function onRequestGet({request,env}){if(!(await isAdmin(request,env)))return json({ok:false,error:'Não autorizado.'},401);if(!env.DB)return json({ok:false,error:'D1 não está ligado a este projeto.'},503);await ensureTable(env);const{results}=await env.DB.prepare('SELECT id,date,name,email,subject,message,read FROM messages ORDER BY date DESC').all();return json({ok:true,messages:results||[]})}
export async function onRequestDelete({request,env}){if(!(await isAdmin(request,env)))return json({ok:false,error:'Não autorizado.'},401);if(!env.DB)return json({ok:false,error:'D1 não está ligado a este projeto.'},503);await ensureTable(env);await env.DB.prepare('DELETE FROM messages').run();return json({ok:true})}
