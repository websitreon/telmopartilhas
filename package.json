{
  "name": "telmo-partilhas",
  "private": true,
  "version": "16.0.0",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name=telmo-partilhas",
    "deploy:preview": "wrangler pages deploy . --project-name=telmo-partilhas --branch=preview"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
