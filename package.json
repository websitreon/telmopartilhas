{
  "name": "telmo-partilhas-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply telmo-partilhas-db --local",
    "db:remote": "wrangler d1 migrations apply telmo-partilhas-db --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
