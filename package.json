{
  "name": "telmo-partilhas",
  "private": true,
  "version": "33.0.0",
  "scripts": {
    "build": "node scripts/check.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}

  "vars": {
    "ADMIN_PASSWORD": "telmo2026"
